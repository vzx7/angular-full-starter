import { async, TestBed } from '@angular/core/testing';

import { ValidationOnBlurDirective } from './validate-onblur.directive';

describe('ValidationOnBlurDirective', () => {
  beforeEach(async(() => {
  }));

});
