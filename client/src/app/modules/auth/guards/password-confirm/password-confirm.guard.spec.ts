import { async, TestBed } from '@angular/core/testing';

import { PasswordConfirmGuard } from './password-confirm.guard';

describe('PasswordConfirmGuard', () => {
  beforeEach(async(() => {
  }));

});
