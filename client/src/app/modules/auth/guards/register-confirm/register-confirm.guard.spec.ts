import { async, TestBed } from '@angular/core/testing';

import { RegisterConfirmGuard } from './register-confirm.guard';

describe('RegisterConfirmGuard', () => {
  beforeEach(async(() => {
  }));

});
